name: macos_clang_19.1

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CMAKE_BUILD_TYPE: Release
  LLVM_VERSION: "19.1.7"
  TOOLS_DIR: "${HOME}/tools"

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Create tools directory
        shell: bash
        run: mkdir -p ${{env.TOOLS_DIR}}

      - name: Detect system architecture
        id: detect_arch
        shell: bash
        run: |
          ARCH=$(uname -m)
          [[ "$ARCH" == "arm64" ]] && TARGET_ARCH="ARM64" || TARGET_ARCH="X64"
          echo "LLVM_ARCH=$TARGET_ARCH" >> $GITHUB_ENV
          echo "Detected architecture: $ARCH"

      - name: Download and Install LLVM
        shell: bash
        run: |
          # 设置 LLVM 安装路径
          LLVM_DIR="${TOOLS_DIR}/llvm"
          mkdir -p "$LLVM_DIR"

          # 下载 LLVM
          LLVM_URL="https://github.com/llvm/llvm-project/releases/download/llvmorg-${{env.LLVM_VERSION}}/LLVM-${{env.LLVM_VERSION}}-macOS-${{env.LLVM_ARCH}}.tar.xz"
          echo "Downloading LLVM from: $LLVM_URL"
          curl -L -o llvm.tar.xz "$LLVM_URL"

          # 解压 LLVM 到临时目录
          TEMP_DIR=$(mktemp -d)
          echo "Extracting LLVM to temporary directory: $TEMP_DIR"
          tar -xf llvm.tar.xz -C "$TEMP_DIR"

          # 检查解压后的顶层目录
          TOP_LEVEL_DIR=$(ls -1 "$TEMP_DIR")
          echo "Top-level directory in LLVM package: $TOP_LEVEL_DIR"

          # 将文件移动到目标路径
          echo "Moving LLVM files to: $LLVM_DIR"
          mv "$TEMP_DIR/$TOP_LEVEL_DIR"/* "$LLVM_DIR"

          # 清理临时目录
          rm -rf "$TEMP_DIR"

          # 检查解压结果
          echo "Contents of LLVM_DIR:"
          ls -l "$LLVM_DIR"

          # 设置环境变量
          echo "PATH=$LLVM_DIR/bin:$PATH" >> $GITHUB_PATH
          echo "LLVM_LIB_PATH=$LLVM_DIR/lib" >> $GITHUB_ENV
          echo "LLVM_INSTALL_PREFIX=$LLVM_DIR" >> $GITHUB_ENV

      - name: Verify LLVM Installation
        shell: bash
        run: |
          echo "LLVM_DIR:"
          ls -l $LLVM_DIR
          echo "LLVM binaries:"
          ls -l ${{env.LLVM_INSTALL_PREFIX}}/bin
          echo "LLVM libraries:"
          ls -l ${{env.LLVM_INSTALL_PREFIX}}/lib
          clang --version

      - name: Download and Install Ninja
        shell: bash
        run: |
          NINJA_VERSION="1.12.0"
          NINJA_URL="https://github.com/ninja-build/ninja/releases/download/v${NINJA_VERSION}/ninja-mac.zip"
          curl -L -o ninja.zip "$NINJA_URL"
          unzip ninja.zip -d ${{env.TOOLS_DIR}}/ninja
          chmod +x ${{env.TOOLS_DIR}}/ninja/ninja
          echo "${{env.TOOLS_DIR}}/ninja" >> $GITHUB_PATH

      - name: Verify Ninja Installation
        shell: bash
        run: ninja --version

      - name: Download and Install CMake
        shell: bash
        run: |
          CMAKE_VERSION="3.30.0"
          CMAKE_URL="https://github.com/Kitware/CMake/releases/download/v${CMAKE_VERSION}/cmake-${CMAKE_VERSION}-macos-universal.tar.gz"
          curl -L -o cmake.tar.gz "$CMAKE_URL"
          tar -xf cmake.tar.gz -C ${{env.TOOLS_DIR}}
          mv ${{env.TOOLS_DIR}}/cmake-${CMAKE_VERSION}-macos-universal ${{env.TOOLS_DIR}}/cmake-${CMAKE_VERSION}
          echo "${{env.TOOLS_DIR}}/cmake-${CMAKE_VERSION}/CMake.app/Contents/bin" >> $GITHUB_PATH

      - name: Verify CMake Installation
        shell: bash
        run: cmake --version

      - name: Configure CMake
        shell: bash
        run: |
          cmake -B build \
            -DCMAKE_BUILD_TYPE=${{env.CMAKE_BUILD_TYPE}} \
            -DCMAKE_C_COMPILER=${{env.LLVM_INSTALL_PREFIX}}/bin/clang \
            -DCMAKE_CXX_COMPILER=${{env.LLVM_INSTALL_PREFIX}}/bin/clang++ \
            -DCMAKE_AR=${{env.LLVM_INSTALL_PREFIX}}/bin/llvm-ar \
            -DCMAKE_RANLIB=${{env.LLVM_INSTALL_PREFIX}}/bin/llvm-ranlib \
            -DCMAKE_LINKER=${{env.LLVM_INSTALL_PREFIX}}/bin/ld.lld \
            -DCMAKE_CXX_FLAGS="-stdlib=libc++ -fuse-ld=lld" \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,-rpath,${{env.LLVM_LIB_PATH}} -L${{env.LLVM_LIB_PATH}}" \
            -DCMAKE_BUILD_RPATH="${{env.LLVM_LIB_PATH}}" \
            -G Ninja

      - name: Build
        shell: bash
        run: |
          cmake --build build --parallel $(sysctl -n hw.logicalcpu)

      - name: Test
        shell: bash
        working-directory: ./build
        run: |
          ctest -T test --output-on-failure --stop-on-failure --no-tests=error --verbose -j $(sysctl -n hw.logicalcpu)
